# 2026-08-22 向 15213472085 发「你好」

## 误判

深链已经打开 `+86 152 1347 2085` 聊天页（标题 `NavigationBar_ConversationHeader`，输入框 `ChatBar_ComposerTextView`），旧选择器读不到标题，当成「没打开」，又点「聊天」返回列表，最后报找不到。

## 修复

- 标题：`NavigationBar_ConversationHeader` / `TitleLabel` / `HeaderViewButton`
- 输入框：`ChatBar_ComposerTextView`
- 已在目标会话则直接发，不再先返回列表
- 模型欠费：4s 超时，401/402/429 冷却 10 分钟，不挡主路径

## 验证

```text
go test ./internal/wda/ ./internal/gateway/ -count=1
ok  wda-farm-gateway/internal/wda       9.396s
ok  wda-farm-gateway/internal/gateway   2.039s

go run ./cmd/wda-probe -wda http://192.168.20.165:8100 -phone 15213472085 -text '你好' -send
send done: phone="15213472085" err=<nil> duration=5.895s
```

未走模型。

完整步骤与禁令见 [whatsapp-send-playbook.md](./whatsapp-send-playbook.md)。
